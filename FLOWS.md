# Folioseam — User Flows

## Happy path
Onboarding → BindingProject → StageTasks → BookSections sew toggles → MaterialLots → Condition before/after → progress → JSON export.

## Permissions
- Photos optional for condition shots; deny → score/notes only.

## Fallbacks
- No sections → progress uses stages only.
